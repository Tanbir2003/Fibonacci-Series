import math
def PS(x):
    s = int(math.sqrt(x))
    return s * s == x
def Fibonacci(n):
    return PS(5 * n * n + 4) or PS(5 * n * n - 4)
num=int(input("How Many Numbers You want to check: "))
for i in range(1, num+1):
 n=int(input("Number: "))
 if (Fibonacci(n)==True):
     print(n, "is a Fibonacci Number")
 else:
     print(n, "is a not Fibonacci Number")
